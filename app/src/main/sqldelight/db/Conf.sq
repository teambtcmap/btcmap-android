import java.time.ZonedDateTime;

CREATE TABLE Conf (
    lastSyncDate TEXT AS ZonedDateTime
);

insert:
INSERT
INTO Conf (lastSyncDate)
VALUES ?;

select:
SELECT *
FROM Conf;

delete:
DELETE
FROM Conf;