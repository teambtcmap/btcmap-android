import org.json.JSONObject;

CREATE TABLE Place (
    id TEXT NOT NULL,
    type TEXT NOT NULL,
    lat REAL NOT NULL,
    lon REAL NOT NULL,
    tags TEXT AS JSONObject NOT NULL
);

CREATE TABLE Location (
    lat REAL NOT NULL,
    lon REAL NOT NULL
);